/** @type {import('next').NextConfig} */
const nextConfig = {
    basePath: "/condev-site",
    output: "export",  // <=== enables static exports
    reactStrictMode: true,
  };
  
  module.exports = nextConfig;
